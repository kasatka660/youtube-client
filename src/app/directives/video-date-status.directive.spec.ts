import { VideoDateStatusDirective } from './video-date-status.directive';

describe('VideoDateStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoDateStatusDirective();
    expect(directive).toBeTruthy();
  });
});
